#!/bin/bash
# 🧪 ПРИМЕРЫ ТЕСТИРОВАНИЯ API - Strikeball Positioning Server

# ═══════════════════════════════════════════════════════════════════════
# УСТАНОВКА ПЕРЕМЕННЫХ
# ═══════════════════════════════════════════════════════════════════════

# Измените IP/порт в зависимости от вашего сервера
SERVER="http://localhost:5000"
# SERVER="http://192.168.1.100:5000"  # Или реальный IP

echo "═══════════════════════════════════════════════════════════════════════"
echo "🧪 Тестирование Strikeball Server API"
echo "═══════════════════════════════════════════════════════════════════════"
echo ""
echo "Сервер: $SERVER"
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 1: Проверка доступности сервера
# ═══════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 1: Проверка доступности сервера"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -I "$SERVER/" 2>/dev/null | head -1
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 2: Получить список якорей
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 2: Получить список якорей (должно быть 4)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -s "$SERVER/api/anchors" | python3 -m json.tool
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 3: Получить список маяков
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 3: Получить список маяков (должно быть 2)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -s "$SERVER/api/beacons" | python3 -m json.tool
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 4: Получить позиции (должно быть пусто)
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 4: Получить позиции (в начале пусто)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -s "$SERVER/api/positions" | python3 -m json.tool
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 5: ОТПРАВИТЬ ТЕСТОВОЕ ИЗМЕРЕНИЕ #1
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 5: Отправить измерение 1 (маяк движется)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Позиция маяка: (10, 10, 0) - угол полигона"
echo ""

curl -s -X POST "$SERVER/api/telemetry/measurement" \
  -H "Content-Type: application/json" \
  -d '{
    "beaconId": 1,
    "distances": [
      {"anchorId": 1, "distance": 14.14},
      {"anchorId": 2, "distance": 42.43},
      {"anchorId": 3, "distance": 56.57},
      {"anchorId": 4, "distance": 42.43}
    ],
    "timestamp": 1709048400000,
    "batteryLevel": 95
  }' | python3 -m json.tool

echo ""
echo "⏳ Ожидаем 1 сек для БД..."
sleep 1

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 6: Проверить позицию маяка 1
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 6: Получить позицию маяка 1"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Ожидаемая позиция: примерно (10, 10, 0) с confidence > 0.8"
echo ""

curl -s "$SERVER/api/positions/1" | python3 -m json.tool
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 7: ОТПРАВИТЬ ИЗМЕРЕНИЕ #2 (другой маяк)
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 7: Отправить измерение 2 (другой маяк)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Позиция маяка: (40, 40, 0) - противоположный угол"
echo ""

curl -s -X POST "$SERVER/api/telemetry/measurement" \
  -H "Content-Type: application/json" \
  -d '{
    "beaconId": 2,
    "distances": [
      {"anchorId": 1, "distance": 56.57},
      {"anchorId": 2, "distance": 42.43},
      {"anchorId": 3, "distance": 14.14},
      {"anchorId": 4, "distance": 42.43}
    ],
    "timestamp": 1709048401000,
    "batteryLevel": 88
  }' | python3 -m json.tool

echo ""
echo "⏳ Ожидаем 1 сек для БД..."
sleep 1

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 8: Получить ВСЕ позиции
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 8: Получить ВСЕ позиции (должно быть 2)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -s "$SERVER/api/positions" | python3 -m json.tool
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 9: История позиций маяка 1
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 9: История позиций маяка 1"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -s "$SERVER/api/positions/history/1" | python3 -m json.tool
echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 10: Обновить якорь (калибровка)
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 10: Обновить якорь (добавить калибровку)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Добавляем смещение +0.1м для якоря 1"
echo ""

curl -s -X PUT "$SERVER/api/anchors/1" \
  -H "Content-Type: application/json" \
  -d '{
    "id": 1,
    "name": "Anchor_1_Calibrated",
    "x": 0.0,
    "y": 0.0,
    "z": 2.0,
    "calibrationOffset": 0.1,
    "status": 0
  }' | python3 -m json.tool

echo ""

# ═══════════════════════════════════════════════════════════════════════
# ТЕСТ 11: Создать новый маяк
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ТЕСТ 11: Создать новый маяк"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

curl -s -X POST "$SERVER/api/beacons" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Player_3_NewBeacon",
    "macAddress": "AA:BB:CC:DD:EE:FF",
    "batteryLevel": 100
  }' | python3 -m json.tool

echo ""

# ═══════════════════════════════════════════════════════════════════════
# ИТОГИ
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "════════════════════════════════════════════════════════════════════════"
echo "✅ ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ!"
echo "════════════════════════════════════════════════════════════════════════"
echo ""
echo "📊 Что было протестировано:"
echo "  ✅ Доступность сервера"
echo "  ✅ Получение якорей и маяков"
echo "  ✅ Отправка измерений и вычисление позиций"
echo "  ✅ Получение текущих и исторических позиций"
echo "  ✅ Обновление якорей (калибровка)"
echo "  ✅ Создание новых маяков"
echo ""
echo "📡 Для WebSocket тестирования:"
echo "  wscat -c ws://$SERVER/hubs/positioning"
echo ""
echo "🎯 Если все работает - сервер готов для клиента!"
echo ""
